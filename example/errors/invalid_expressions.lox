fun main() {
    // an example set of invalid statements
    // that should resolve cleanly (not "bleed" errors onto the next line)

    // expressions

    /// expected a valid expression ------v
    var semicolon_instead_of_expression = ;

    /// ---------------------vvvvvv expected a valid expression
    var invalid_expression = number;

    /// expected a valid expression ---------v
    var premature_semicolon_in_group = (15 * ;

    /// expected a valid expression ------------v
    var premature_semicolon_in_group_2 = (12 * (;

    /// expected a valid expression -----v
    var premature_paren_in_group = (12 * ) / 6;

    ///   v--- expected a valid expression, found rbrace
    ///   v--- expected a semicolon, found rbrace
    { 7 * }



    // function calls
    /// statement end before right paren closing
    sf(;
    /// arguments are not seperated by a comma
    qe(1 15);
    /// trailing comma in arguments
    burger(6, 7,);

    /// argument is not an expression
    call(number);
}
